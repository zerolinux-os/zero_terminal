# =============================================================================
# ZeroLinux Plugin: system — init.zsh  (v2.1.1)
# =============================================================================

plugin_init() {
  zl::log::debug "plugin[system]: init OK"
}

plugin_register_commands() {
  zl::log::debug "plugin[system]: commands registered"
}
