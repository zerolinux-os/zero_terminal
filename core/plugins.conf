# ZeroLinux Plugin Configuration — core/plugins.conf
# One plugin name per line. Comments start with #.
# Manage with: zl enable/disable/install/remove plugin <n>
#
# Available plugins: git docker arch system example
git
system
# docker   ← uncomment if Docker is installed
# arch     ← uncomment if running Arch Linux
